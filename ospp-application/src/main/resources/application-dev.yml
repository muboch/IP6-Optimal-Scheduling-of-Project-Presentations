spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/osppdb
    username: ospp
    password: Change_me_2020
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
ospp:
  solver: ch.fhnw.ip6.cpsolver.Solver
  timelimit: 180
  testmode: false

server:
  address: 0.0.0.0