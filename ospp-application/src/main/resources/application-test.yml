spring:
  profiles: test
  datasource:
    url: jdbc:mysql://docker-mysql:3306/osppdb
    username: ospp
    password: Change_me_2020
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create


ospp:
  solver: ch.fhnw.ip6.ortoolssolver.Solver
  timeLimit: 300 # in seconds
  testmode: false
